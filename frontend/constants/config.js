// NOTE: Use your machine's local IP address (e.g., 192.168.1.5:3000) 
// if running on a physical device, NOT localhost. 
// If using an emulator, 'http://10.0.2.2:3000' usually works for Android, 
// and 'http://localhost:3000' for iOS.

const BASE_URL = 'http://localhost:3000/api'; 

export const API_BASE_URL = BASE_URL;